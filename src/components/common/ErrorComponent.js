import React from 'react';

const ErrorComponent = () => {
  return (
    <div className="p-5 bg-red-100 rounded-lg text-error">
      Look like component is error
    </div>
  );
};

export default ErrorComponent;
